[project]
name = "datacommons-platform"
dynamic = ["version"]
description = 'The Data Commons Platform is an open source semantic graph database for modeling, querying, and analyzing interconnected data.'
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
keywords = []
authors = []
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: Implementation :: PyPy",
]
dependencies = [
  "datacommons-cli",
]

[dependency-groups]
dev = [
  {include-group = "lint"},
  {include-group = "test"},
  "datacommons-devtools",
]
lint = [
  "pre-commit>=4.5.1",
  "ruff>=0.15.0",
]
test = [
    "pytest>=9.0.3",
    "datacommons-client>=2.1.6",
    "datacommons-api",
    "datacommons-db",
    "datacommons-schema",
    "datacommons-admin",
    "google-cloud-spanner>=3.40.0",
    "google-cloud-storage>=2.14.0",
    "pyyaml>=6.0",
    "requests>=2.31.0",
]



[project.urls]
Documentation = "https://github.com/datacommonsorg/datacommons#readme"
Issues = "https://github.com/datacommonsorg/datacommons/issues"
Source = "https://github.com/datacommonsorg/datacommons"

[tool.uv.sources]
datacommons-api = { workspace = true }
datacommons-db = { workspace = true }
datacommons-schema = { workspace = true }
datacommons-cli = { workspace = true }
datacommons-admin = { workspace = true }
datacommons-devtools = { workspace = true }

[tool.uv.workspace]
members = [
    "packages/*",
]

[tool.coverage.run]
source = ["datacommons"]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
    "pass",
    "@abstractmethod",
]

[tool.ruff.lint]
# Visit https://docs.astral.sh/ruff/rules/ for detailed rule descriptions.
extend-select = [
  "ERA",  # removes commented-out code from Python files.
  "FAST", # linting FastAPI specifics
  "YTT",  # Flake8 checks for misuse of `sys.version` or `sys.version_info`
  "ANN",  # Flake8 Type Annotation Checks
  "S",    # Automated security testing with bandit and flake8.
  "BLE",  # A flake8 extension that checks for blind except: statements
  "FBT",  # A flake8 plugin to detect boolean traps.
  "B",    # flake8-bugbear finds likely bugs and design problems.
  "A",    # flake8-builtins detects shadowed python builtins
  "COM",  # flake8 lint for trailing commas.
  "C4",   # flake8 plugin to help write better list/set/dict comprehensions
  "ISC",  # flake8 plugin to encourage correct string literal concatenation
  "LOG",  # flake8 plugin checks for issues with standard library logging module
  "G",    # flake8 plugin to encourage better formatting of built-in logging strings
  "PIE",  # flake8 misc lints
  "PT",   # flake8 plugin checks common pitfalls with pytest-based tests
  "RET",  # encourages good return practices
  "SIM",  # flake8-simplify checks common simplification patterns
  "TID",  # flake8-tidy-imports encourages tidier imports
  "TD",   # flake8-todos lints TODOs throughout the project for consistency
  "TC",   # flake8 plugin for managing type-checking imports & forward references
  "ARG",  # flake8-unused-arguments warns on unused function arguments
  "PTH",  # flake8-use-pathlib encourages pathlib over os.path
  "I",    # isort sorts Python imports
  "N",    # check PEP-8 naming conventions
  "PERF", # Pylint extension with performance anti-patterns
  "E",    # pycodestyle errors
  "W",    # pycodestyle warnings
  "F",    # pyflakes analyzes and detects various errors
  "PL",   # pylint
  "Q",    # defaults to double quotes
  "UP", # Encourage syntax supported by newer python versions
  # "CPY", # check for copyright headers # uncomment once graduated from preview
]
extend-ignore = [
  "TD003",  # missing-todo-link: Missing issue link for this TODO
  "PLC",    # pylint conventions
  "PLR",    # pylint refactor
  "COM812", # missing-trailing-comma may conflict with formatter
  "E501",   # line-too-long
]

[tool.ruff.lint.per-file-ignores]
"deploy/scripts/**/*.py" = [
    "S603",   # Subprocess call check in automation scripts
    "S607",   # Partial executable path in automation scripts
]
"packages/*/datacommons_*/migrations/migration_scripts/*.py" = [
    "N999",   # Allow timestamp prefixes (e.g. 20260817000000_*.py) for migration scripts
]
"packages/datacommons-devtools/datacommons_devtools/migrations/templates/*.py" = [
    "ERA001", # Allow example commented-out code in migration template
    "F401",   # Allow pre-imported modules in migration template
]
"**/tests/**/*.py" = [
    "S101",   # Allow using `assert` in pytests
    "S105",   # Allow hardcoded mock passwords/tokens in tests
    "S106",   # Allow hardcoded mock token arguments in tests
    "S108",   # Probable insecure usage of temp file/dir
    "S110",   # Allow try-except-pass in data parsing helpers
    "S602",   # Subprocess call with shell=True for interactive fixes
    "S603",   # Subprocess call check in test runners
    "S607",   # Partial executable path in test runners
    "ANN001", # Ignore missing type annotations in test files
    "ANN002", # Ignore missing type annotations for *args in test helpers
    "ANN003", # Ignore missing type annotations for **kwargs in test helpers
    "ANN201", # Ignore missing return types in public test functions
    "ANN202", # Ignore missing return types in private test functions
    "ANN204", # Ignore missing return types in __init__
    "ANN401", # Dynamically typed Any in test schemas
    "ARG001", # Allow unused arguments in mock functions
    "ARG002", # Allow unused pytest fixture arguments in test methods
    "ARG005", # Allow unused lambda arguments in mocks
    "BLE001", # Allow broad exception catching in test resilience
    "E402",   # Module level import not at top of file (sys.path config)
    "FBT001", # Boolean-typed positional arguments
    "FBT002", # Allow boolean default positional arguments in test mocks
    "PLR2004",# Allow magic values in test assertions
    "PLW0602",# Using global for reporter across hooks
    "PLW0603",# Using global for reporter across hooks
    "PTH103", # Allow os.makedirs
    "PTH123", # Allow builtin open() in test loaders
]

# Explicitly define PyPI as the default index
[[tool.uv.index]]
url = "https://pypi.org/simple"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
# Tell setuptools this is a meta-package and to stop looking for code
packages = []

[tool.setuptools.dynamic]
version = {file = "VERSION"}

[tool.pytest.ini_options]
pythonpath = [".", "deploy/scripts"]
testpaths = ["packages", "deploy/scripts/tests"]
markers = [
    "cloud_only: mark test or class to run only against live GCP cloud targets.",
]

